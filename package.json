{
  "name": "nfc-review-card",
  "version": "1.0.0",
  "private": true,
  "description": "NFC Review Card — scan first, then attach business",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20 <25"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "express": "^5.1.0"
  }
}
